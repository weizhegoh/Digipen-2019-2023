#include <iostream>

/*
class A
{
    int n;  // keep it private

public:
    A() :n(0) {}

    void set(int x)
    {
        this->n = x;
    }

    friend std::ostream& operator <<(std::ostream& os, const A& rhs)
    {
        os << rhs.n << std::endl;
        return os;
    }
};

int main(void)
{
    A a;
    a.set(3);
    std::cout << a;
    return 0;
}
*/